# Application Agent Rules

- Never embed infrastructure or OpenAI secrets.
- Validate all API, widget, extension, and postMessage payloads.
- Preserve accessibility, origin isolation, CSP, and memory-only session-token requirements.
- Keep shared code in packages rather than importing across applications.
