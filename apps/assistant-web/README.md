# apps/assistant-web

Next.js assistant application scaffold for the embedded customer-facing chat experience.

Run locally after installing the workspace dependencies:

```bash
pnpm --filter @icakb/assistant-web dev
```
