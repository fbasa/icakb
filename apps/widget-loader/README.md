# apps/widget-loader

TypeScript widget-loader package scaffold for the embeddable assistant launcher.

Run local verification after installing workspace dependencies:

```bash
pnpm --dir apps/widget-loader typecheck
pnpm --dir apps/widget-loader test
```
