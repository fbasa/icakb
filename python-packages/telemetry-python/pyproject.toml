[project]
name = "telemetry-python"
version = "0.0.0"
requires-python = "==3.13.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/telemetry_python"]
