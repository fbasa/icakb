# Service Agent Rules

- Keep domain, application, and infrastructure layers separate.
- Do not allow provider SDK types to escape adapters.
- Use typed configuration, structured errors, tenant-scoped repositories, and content-redacted telemetry.
- Every external dependency requires timeouts and explicit error translation.
