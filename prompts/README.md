# Prompt Library

All prompt files are drafts until implemented by their assigned atomic tasks. Register every production prompt in `manifest.yaml`, validate variables, associate evaluations, and record an explicit version.
