# Workflows

Workflow implementations are Phase 0 atomic tasks. Required workflows include pull-request CI, CodeQL, security scanning, development deployment, staging promotion, production promotion, and scheduled evaluations.
