# Infrastructure Agent Rules

- Use least privilege, isolated environments, encrypted state, immutable artifacts, and workload identity.
- Never place secrets in Terraform variables committed to the repository.
- Infrastructure changes require validation, scanning, rollback considerations, and operations documentation.
